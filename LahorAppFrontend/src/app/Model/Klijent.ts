export class Klijent{
  public aktivan:boolean;
  public clanskaKartica:boolean;

   constructor(aktivan:boolean,clanskaKartica:boolean)
  {
    this.aktivan=aktivan;
    this.clanskaKartica=clanskaKartica;
  }
}
